server:
  port: 8182

debug:
  enabled: true

logging:
  level:
    com.e.bambi: DEBUG
    reactor.kafka: DEBUG
    io:
    #      r2dbc: DEBUG
    #      r2dbc.pool: DEBUG
    #      r2dbc.postgresql: DEBUG
    org:
      #      apache.kafka: DEBUG
      springframework: DEBUG
      springframework.r2dbc: DEBUG
#      springframework.data.r2dbc: TRACE

inventory-service:
  order.inventory.cancel_reservation-topic-name: order.inventory.cancel_reservation
  order.inventory.reserve-topic-name: order.inventory.reserve

spring:
  webflux:
    base-path: /api
  profiles:
    active: native
  application:
    name: inventory-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${JWT_ISSUER_URI:http://localhost:8081/realms/bambi}
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
  r2dbc:
    url: r2dbc:postgresql://${DATASOURCE_URL:localhost:5433}/inventory-service
    username: ${DATASOURCE_USERNAME:user}
    password: ${DATASOURCE_PASSWORD:password}
  flyway:
    url: jdbc:postgresql://${DATASOURCE_URL:localhost:5433}/inventory-service
    locations: classpath:db/migration

kafka-config:
  bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:19092, localhost:29092, localhost:39092}
  schema-registry-url: ${KAFKA_SCHEMA_REGISTRY_URL:http://localhost:18081}

kafka-consumer-config:
  inventory-service-consumer-group-id: inventory-service-consumer
  specific-avro-reader: true
  key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
  value-deserializer: io.confluent.kafka.serializers.KafkaAvroDeserializer
  enable-auto-commit: false
  auto-offset-reset: earliest

springdoc:
  api-docs.enabled: false
  swagger-ui:
    showExtensions: true
    url: /openapi.yaml

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true