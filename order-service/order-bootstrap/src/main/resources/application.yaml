server:
  port: 8181

debug:
  enabled: true

logging:
  level:
    com.e.bambi: DEBUG
    reactor.kafka: DEBUG
    io:
    #      r2dbc: DEBUG
    #      r2dbc.pool: DEBUG
    #      r2dbc.postgresql: DEBUG
    org:
      #      apache.kafka: DEBUG
      springframework: DEBUG
      springframework.r2dbc: DEBUG
#      springframework.data.r2dbc: TRACE

order-service:
  inventory.reserved-topic-name: inventory.reserved
  inventory.reservation_failed-topic-name: inventory.reservation_failed
  inventory.reservation_cancelled-topic-name: inventory.reservation_cancelled
  payment.validated-topic-name: payment.validated
  payment.validation_failed-topic-name: payment.validation_failed

spring:
  webflux:
    base-path: /api
  profiles:
    active: native
  application:
    name: order-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${JWT_PROVIDER_URI:http://localhost:8081/realms/bambi}
  redis:
    host: ${REDIS_HOTS:localhost}
    port: ${REDIS_PORT:6379}
  r2dbc:
    url: r2dbc:postgresql://${DATASOURCE_URL:localhost:5434}/order-service
    username: ${DATASOURCE_USERNAME:user}
    password: ${DATASOURCE_PASSWORD:password}
  flyway:
    url: jdbc:postgresql://${DATASOURCE_URL:localhost:5434}/order-service
    locations: classpath:db/migration

kafka-config:
  bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:19092, localhost:29092, localhost:39092}
  schema-registry-url: ${KAFKA_SCHEMA_REGISTRY_URL:http://localhost:18081}

kafka-consumer-config:
  order-service-consumer-group-id: order-service-consumer
  specific-avro-reader: true
  key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
  value-deserializer: io.confluent.kafka.serializers.KafkaAvroDeserializer
  enable-auto-commit: false
  auto-offset-reset: earliest

springdoc:
  api-docs.enabled: false
  swagger-ui:
    showExtensions: true
    url: /openapi.yaml

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true