{{- if .Values.secrets.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "app.fullname" . }}-secret
type: Opaque
data:
  {{- range $key, $value := .Values.secrets.credentials }}
    {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}
